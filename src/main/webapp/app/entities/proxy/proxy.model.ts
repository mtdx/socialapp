export class Proxy {
    constructor(
        public id?: number,
        public host?: string,
        public port?: number,
        public username?: string,
        public password?: string,
    ) {
    }
}
